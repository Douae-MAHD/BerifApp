name: berif_app
description: Application professionnelle de gestion de travaux techniques
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 🔥 Firebase (Versions synchronisées pour éviter les conflits)
  firebase_core: ^3.10.1
  cloud_firestore: ^5.6.12
  firebase_storage: ^12.4.10 # <-- Mis à jour pour compatibilité

  # 🎨 UI & Design
  google_fonts: ^6.2.1
  flutter_riverpod: ^2.5.1 # <-- Version stable (la 3.0 est encore en beta)

  # 🛠 Utilitaires
  intl: ^0.19.0 # <-- Version stable standard
  file_picker: ^8.1.2
  flutter_pdfview: ^1.3.2
  path_provider: ^2.1.3
  path: ^1.9.0 # <-- Indispensable pour manipuler les noms de fichiers PDF

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true